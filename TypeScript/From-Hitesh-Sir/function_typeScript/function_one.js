function addTwo(num) {
    // num.toLowerCase() ; 
    return num;
}
addTwo(1);
var loginUser = function (name, email, isPaid) {
    if (isPaid === void 0) { isPaid = false; }
    console.log(name, email, isPaid);
};
loginUser("harsh", "harsh@123");
