// What is the type Inference in typescript? 

// In simple terms, Type Inference is TypeScript's ability to automatically determine the type of a variable, function return, or expression based on its initialization or usage, without you explicitly having to write out the type annotation.

// Think of it like this: TypeScript is smart enough to figure things out on its own. While you can explicitly tell TypeScript the type of everything (which is called type annotation), you often don't have to, thanks to type inference. This makes your code cleaner and faster to write, without sacrificing the benefits of static typing.